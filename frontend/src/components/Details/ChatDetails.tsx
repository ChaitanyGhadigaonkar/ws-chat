import Grid from "../Grid";

const ChatDetails = () => {
  return (
    <Grid
      container
      size="grow"
      sx={{
        backgroundColor: "#FFF",
        boxShadow: "rgba(99, 99, 99, 0.2) 0px 2px 8px 0px",
        borderRadius: "0.5rem",
      }}
    >
      Chat Details
    </Grid>
  );
};

export default ChatDetails;
